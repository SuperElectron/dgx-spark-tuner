command: |
  vllm serve nvidia/Qwen3.6-35B-A3B-NVFP4 \
    --host {host} \
    --port {port} \
    --trust-remote-code \
    --kv-cache-dtype fp8 \
    --attention-backend flashinfer \
    --moe-backend marlin \
    --gpu-memory-utilization {gpu_memory_utilization} \
    --max-model-len {max_model_len} \
    --max-num-seqs {max_num_seqs} \
    --max-num-batched-tokens {max_num_batched_tokens} \
    --enable-chunked-prefill \
    --async-scheduling \
    --enable-prefix-caching \
    --load-format fastsafetensors \
    --reasoning-parser qwen3 \
    --tool-call-parser qwen3_xml \
    --enable-auto-tool-choice \
recipe_version: '1'
model: nvidia/Qwen3.6-35B-A3B-NVFP4
solo_only: false
container: vllm-node
cluster_only: false
env:
  VLLM_MARLIN_USE_ATOMIC_ADD: '1'
description: Qwen3.6-35B-A3B-NVFP4 single-node — R24 ARM 3, shipped recipe with MTP speculative decoding removed. Diagnostic probe of the 0.0% prefix-cache hit rate. NOT the shipped config.
name: Qwen3.6-35B-A3B-NVFP4-solo-r24-specoff
defaults:
  port: 8000
  max_num_batched_tokens: 65536
  max_model_len: 32768
  gpu_memory_utilization: 0.8
  max_num_seqs: 4
  host: 0.0.0.0
