.cache/
.claude/box.json
.DS_Store
__pycache__/
.omc/
# Experiment run data stays local; the hypothesis and its conclusion are tracked.
# Git cannot re-include a file inside an excluded directory, so the hypothesis
# directories must be un-excluded first, then their run data excluded again.
research/*/experiments/*
!research/*/experiments/*/
research/*/experiments/*/*
!research/*/experiments/*/HYPOTHESIS.md
!research/*/experiments/*/EXPERIMENT.md
# A candidate recipe records the exact serve configuration a run used — it is
# the reproducibility record, so it is tracked wherever it sits. Measurements
# (state.yaml, round-tmp.*, runs/, logs) stay local.
!research/*/experiments/*/*/
research/*/experiments/*/*/*
!research/*/experiments/*/*/recipe-*.yaml
!research/*/experiments/*/*/PROVENANCE.md
!research/_template/experiments/.gitkeep
research/milestones/
research/*/QUEUE.md
